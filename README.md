# SHIELD-scanner
SHIELD Scanner is a GitHub Actions–powered web interface that executes SHIELD Framework scans with authorization handling, mode selection, containerized execution, and JSON-driven HTML reporting.
